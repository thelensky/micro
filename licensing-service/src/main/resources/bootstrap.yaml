spring:
  application:
    name: license-service
  config:
    import: optional:configserver:http://localhost:8071
  cloud:
    config:
      retry:
        initial-interval: 1000   # 1 секунда
        max-interval: 2000
        multiplier: 1.5
        max-attempts: 10

server:
  port: 8081